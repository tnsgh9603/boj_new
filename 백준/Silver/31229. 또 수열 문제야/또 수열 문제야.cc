#include <bits/stdc++.h>

#define fastio ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
using namespace std;
int main() {
    fastio;
    int N, A = -1;
    cin >> N;
    while (N--) {
        A += 2;
        cout << A << " ";
    }
    return 0;
}